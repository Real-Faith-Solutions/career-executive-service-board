<?php

namespace App\Definitions;

/**
 * Class AppDefinitions
 *
 * Define the common values that can be used across the whole code base
 *
 * @package App\Definitions
 */
class AppDefinitions
{
    const DEFAULT_CESOS_ID      = array(1, 2, 3, 4, 5, 6);
    const DEFAULT_CSEE_ID       = array(8);
    const DEFAULT_ELIGIBLE_ID   = array(7);
}
